describe('child-siblings', () => {
    it('exists', () => {
        const calledWith = [];
        require('./index.js')((arg) => calledWith.push(arg));
        expect(calledWith).toEqual(['child-siblings', 'child-root']);
    });
});