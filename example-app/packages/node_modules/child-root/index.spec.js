describe('child-root', () => {
    it('exists', () => {
        const calledWith = [];
        require('./index.js')((arg) => calledWith.push(arg));
        expect(calledWith).toEqual(['child-root']);
    });
});