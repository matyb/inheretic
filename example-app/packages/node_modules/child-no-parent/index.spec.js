describe('child-no-parent', () => {
    it('exists', () => {
        const calledWith = [];
        require('./index.js')((arg) => calledWith.push(arg));
        expect(calledWith).toEqual(['child-no-parent']);
    });
});