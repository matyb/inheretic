- add optional property for template file to seek in child modules (ex. package.json.template?)
- handle existence of property, like arrays and objects and not clobbering if a property is already there, etc.
