describe('child-siblings-twin', () => {
    it('exists', () => {
        const calledWith = [];
        require('./index.js')((arg) => calledWith.push(arg));
        expect(calledWith).toEqual(['child-siblings-twin', 'child-root']);
    });
});